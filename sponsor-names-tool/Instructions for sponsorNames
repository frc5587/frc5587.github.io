-=- INSTRUCTIONS FOR USING THE SPONSOR NAMES TOOL -=-

1) Edit the razooNames.txt (which is in the same folder as this text file) as needed

2) Navigate to the file that holds sponsorNames.py in cmd/terminal

3) Type "python sponsorNames.py" (assuming you have python installed, otherwise, install it)

4) Copy the output from the console - sucks for you Windows users have fun copying


^* NOTE: all of the names should be pre-alphabetized and filtered through for repeats before
being but in razooNames.txt